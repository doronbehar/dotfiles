#!/bin/sh
ps aux | awk -v PROCESS="$1" '
	{
		if ($0 ~ PROCESS && $0 !~ /memoryusage/ && $0 !~ /awk/){
			count+=$4
		}
	}
	END {
		if (count == "")
			print "No process "PROCESS" was found"
		else
			print count
	}
'
