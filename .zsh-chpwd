function chpwd() {
	chpwd.awk ~/.local/share/tasks/contexts.txt
	if [ $? -eq 1 ]; then
		if [[ "${_HACKING_FROM_HOME}" == "true" ]]; then
			task context home
		else
			task context default
		fi
	fi
}
chpwd

