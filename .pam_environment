# Browser (for xdg-open)
BROWSER DEFAULT="firefox"

# Pager 
PAGER DEFAULT="less"
LESS DEFAULT="-X -x4 -r -i"
LESSHISTFILE DEFAULT="@{HOME}/.local/share/less-history"

# SSH
SSH_AUTH_SOCK DEFAULT="${XDG_RUNTIME_DIR}/ssh-agent.socket"

# GnuPG
PINENTRY_USER_DATA DEFAULT="tty"

# info
INFO_PRINT_COMMAND DEFAULT="@{HOME}/.bin/info-print"

# XDG_UTILS
XDG_UTILS_DEBUG_LEVEL DEFAULT="1"

# translate-shell
HOME_LANG DEFAULT=he
TARGET_LANG DEFAULT=he

# mesa shader cache
MESA_GLSL_CACHE_DIR=/var/cache/mesa_shader_cache/doron
